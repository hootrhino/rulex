name: BuildArm32

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: BuildArm32 && Test
        run: |
              docker build -f ./.github/workflows/Dockerfile_arm32v7 . 
